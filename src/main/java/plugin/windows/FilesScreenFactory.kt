package plugin.windows

import com.intellij.openapi.project.Project
import com.intellij.openapi.wm.ToolWindow
import com.intellij.openapi.wm.ToolWindowFactory

class FilesScreenFactory : ToolWindowFactory {

    override fun createToolWindowContent(project: Project, toolWindow: ToolWindow) {
//        PsiDocumentManager.getInstance(project).getPsiFile(project.)
//        val myToolWindow = FilesScreen(toolWindow)
//        val contentFactory = ContentFactory.SERVICE.getInstance()
//        val content = contentFactory.createContent(myToolWindow.content, "", false)
//        toolWindow.contentManager.addContent(content)
    }
}
